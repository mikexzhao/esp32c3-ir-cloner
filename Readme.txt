1. Implement feature to DO  IR code cloner with ESP32-C3 dev module. 
2. It implements codes with ESP-IDF, not arduino. 
3. The code should have two main functions:
    a. IR code receiver
    b. IR code sender
    c. Store received codes in NVS memory
    d. Send stored codes on user request


